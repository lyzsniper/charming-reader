info:
  name: Get Current Model Info
  type: http
  seq: 29

http:
  method: GET
  url: "{{baseUrl}}/model-configurations/current/info"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/model-configurations/current/info"
      method: GET
    response:
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
