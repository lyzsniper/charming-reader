info:
  name: Suggest Skills
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/skills/suggest"
  body:
    type: json
    data: |-
      {
        "message": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/skills/suggest"
      method: POST
      body:
        type: json
        data: |-
          {
            "message": ""
          }
    response:
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 422 Response
    description: Validation Error
    request:
      url: "{{baseUrl}}/skills/suggest"
      method: POST
      body:
        type: json
        data: |-
          {
            "message": ""
          }
    response:
      statusText: Unprocessable Entity
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "detail": [
              {
                "loc": [],
                "msg": "",
                "type": ""
              }
            ]
          }
